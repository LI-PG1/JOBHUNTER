# 简历生成助手 — 产品设计学习汇总（源自 MS-Agent，2026-08-08）

> **Purpose**: 面向 简历生成助手 Creator 的学习改进文档。汇总作者在同族产品 **MS-Agent / MS-Agent-Lite**（固定流水线、本地优先、简历+JD → 面试材料）于 **2026-08-08** 三个版本（v0.4.14 / v0.4.15 / v0.4.16）中的产品界面与交互改动，提炼**可迁移的产品设计原则、实现模式、验证方法论与踩坑教训**，并给出 简历生成助手 可直接执行的改进清单。
>
> **2026-08-08 二次更新（执行回执）**: 行动清单已对照 简历生成助手 现状逐条核对，新增 **§8 执行回执**——标注每条原则的落地状态（✅ 已满足 / ⚠️ 部分 / ❌ 未执行）与证据位置，并汇总 R19~R22 迭代中与本文原则对应的新增能力与验证纪律。
>
> **Related lineage**: 与 `learning-summary-open-source-research.md`（2026-08-06，开源竞品研究）配套。该研究已确认「本地优先」与「质量门」是 简历生成助手/MS-Agent 的差异化定位；本文则聚焦**界面与交互的产品 sense**——这部分不来自竞品，而来自用户对自有产品的一轮轮打磨。

---

## 0. 一句话总纲

> **默认用户不会仔细看、甚至不会去看使用说明——所以用户界面的引导必须清晰；界面只告诉用户「该做什么」，不渲染「产品承诺不做什么」；开发视角的概念区分（平台/模型/配置名称……）用交互表达，而不是写解释文字。**

---

## 1. 改动全景（2026-08-08 做了什么）

| 版本 | 主题 | 核心改动 |
|---|---|---|
| **v0.4.14** | 产品界面精修（去伪/收敛） | ① 去除「默认/推荐」提示（不告诉用户什么是默认，直接呈现最优项）② Lite 版移除「模型能力选择」（纯文本版一个文本模型即可，删伪步骤）③ 岗位名称等提示精简（效果说明收敛到右侧常驻提醒栏）④ 简历上传提示精简不重复 ⑤ 「JD 链接与文本二选一」修正为可同时提供 ⑥ 参考网址/信息提示统一迁至常驻提醒栏 ⑦ 删除「联系方式（可选）」伪选项（实为每次默认带的水印） |
| **v0.4.15** | 删伪功能 + 引导补位 | ① 删除「填入示例（先试试效果）」按钮 ② 隐私声明类文案移出 UI（5 处：不会上传/不会保存/仅保存在本机）③ 新增必填项缺失定位引导（滚动+红框高亮+「已定位到第一个缺失项」）④ 新增左栏常驻「当前该做什么」动态引导行（4 态自动切换） |
| **v0.4.16** | 配置交互升级 | ① API Key 输入框提示随所选厂商+模型动态变化（显示该厂商 Key 格式示例 + 当前模型名）② 主界面改「厂商 + 模型」两级联动选择（12 厂商预置常见模型：DeepSeek 仅 v4-flash/v4-pro，其余 3~4 个）③ 「平台/模型/配置名称」概念区分文字全部移出界面（用交互联动区分）④ 全界面「选平台」统一改「选厂商」 |

---

## 2. 可迁移的产品设计原则（核心，每条含 MS-Agent 实例 + 简历生成助手 应用）

### P1. UI 只呈现「用户要做什么」，不渲染「产品承诺不做什么」

- **MS-Agent 实例**：主界面原有多处「所有数据仅保存在本地，不会上传到任何服务器 / 简历仅用于本次生成，不会保存到本机 / Key 仅保存在你本机 config.json」等声明。用户判定：**这类文字放在界面反而让用户警觉**（暗示存在风险才需要承诺），全部移出 UI，仅保留在 README 与使用指南。保留的是**可操作安全建议**（「Key 请勿发到公开渠道，泄露立即去平台重置」）——那是行为指导，不是承诺。
- **简历生成助手 应用**：README 写了「本地单用户：所有数据保存在本机，无云服务、无账户，数据不出机」——这是文档职责，没问题。但**检查 web 界面里是否有同类声明文案**（「数据不出机/不上传/仅保存在本地」），有则移出 UI。

### P2. 默认用户不读文档：引导必须在界面内自解释

- **MS-Agent 实例**：用户的原始动机句——「默认用户不会真的去仔细看甚至不会去看使用说明，所以用户界面的引导必须清晰」。据此新增左栏**常驻动态引导行（railNote）**，永远告诉用户「现在该做什么、下一步是什么」。
- **简历生成助手 应用**：简历工作台的左栏/顶部应有类似的常驻引导，随表单进度自动切换（开始 → 下一步 → 生成中 → 完成），而不是依赖教程/提示文案。

### P3. 常驻「当前步骤 + 下一步」指示器 > 教程/提示文案

- **MS-Agent 实例**：两级指引结构——**页面级主线**（右栏底部常驻「使用主线：① 配置 API Key → ② 填写材料 → ③ 一键生成」）+ **卡片级子流程步骤条**（配置卡片顶部 flow-steps，把「配置 Key」拆成 1 粘贴钥匙 → 2 选择能力 → 3 保存验证 → 4 高级设置（可选））+ **流程级动态引导行**（左栏 railNote 4 态：`开始：第 1 步…` → `下一步：第 2 步…` → `生成中：…` → `完成：…`）。
- 对标结论（Jovie UX issue / Static Forms / UXHeat / Nielsen 10 启发式）：**渐进式表单 + 常驻步骤指示器**比教程文案有效得多；用户不读文档，所有引导必须在界面内自解释。三层指示器（主线 / 步骤条 / 动态行）让用户在**任何时刻、任何位置**都知道「整个任务怎么走、这个卡片怎么填、现在该做什么」。

### P4. 错误恢复必须指出具体字段，并带用户到现场

- **MS-Agent 实例**：点「一键生成」漏填必填项时——红字列出全部缺失项 + **自动滚动定位第一个缺失字段 + 红框高亮 2.2s** + 文案「——已定位到第一个缺失项」。而不是只弹一行红字让用户猜改哪里。
- **简历生成助手 应用**：简历表单必填项（基本信息/经历/JD）同样适用。注意校验**顺序**：先引导最前置的配置（无 Key 时优先引导去配 Key），再校验表单字段——这是「一阶引导」的产品正确行为，不是 bug。

### P5. 概念区分用交互表达，不给用户看开发说明

- **MS-Agent 实例**：原 HELP 有「概念区分：平台=你从哪家服务商申请 API Key；模型=该平台上具体用的模型；配置名称=你给这套配置起的标签」。用户判定：**这是让开发做区分的，不是给用户看的**。解法不是删掉说明，而是把概念做进交互：主界面「① 选择厂商 → ② 选择模型（自动列出常见模型）→ ③ 粘贴 API Key」，选完自动填好配置名称/模型名/接口地址——用户操作一遍就自然理解三者关系，无需读解释。
- **简历生成助手 应用**：凡是「××是什么、××和××的区别」式的界面文字，都先问一句：能不能做成交互让用户自然理解？开发内部概念（字段语义、术语体系）不要平铺到 UI。

### P6. 不告诉用户「默认/推荐」，直接呈现最优项

- **MS-Agent 实例**（v0.4.14）：去掉「DeepSeek（默认）」「绝大多数模型选它（最稳妥）」等字眼。规则：**不告诉用户什么是默认/推荐**——打开即是 DeepSeek，就是最直接的表达；「推荐」类主观词一律不用。
- **简历生成助手 应用**：若有「推荐模型/默认选项/最稳妥」类文案，删掉，直接设好默认选中。

### P7. 删除伪选项（实际是固定行为的东西不要做成用户选项）

- **MS-Agent 实例**（v0.4.14）：界面曾有「联系方式（可选）」输入框，实则每次生成都固定带上联系方式水印——是个伪选项。删除该字段，水印固定为「联系作者 llxstupg@163.com」。
- **简历生成助手 应用**：审视表单里每个「可选」字段——如果它其实每次都生效/固定，就是伪选项，删掉或做成固定行为。

### P8. 主 CTA 旁不摆演示按钮

- **MS-Agent 实例**（v0.4.15）：删除主按钮旁的「填入示例（先试试效果）」按钮。四条理由：① 双按钮竞争 = 行为噪音，新用户困惑先点哪个；② 演示数据有**误提交风险**（不改假数据直接点生成，浪费 token 还困惑）；③ 示例价值已被各输入框 placeholder 覆盖（如「如 腾讯-Agent开发实习生」）；④ 违背「引导告诉用户此刻做什么」原则——示例按钮本质是把文档塞回界面。
- 对标结论：面试/简历类产品（RealtimeCopilot、Interview Sidekick、Google Interview Warmup 等）**没有成功产品用示例按钮做引导**，靠「免费体验额度 + 注册后直接可用的主流程」。

### P9. 字段提示精简、不重复：细节说明收敛到常驻位置

- **MS-Agent 实例**（v0.4.14）：同一字段的「效果：…」说明曾在输入框旁和右侧提醒栏出现两份，主界面删一份，统一收敛到聚焦时右栏显示的常驻提醒；简历上传提示从长段落精简为一行格式说明（「支持格式：PDF（文字版）/ Word（docx）/ txt / markdown」），扫描件警告只在右侧对应字段帮助里出现一次。
- **简历生成助手 应用**：同一信息在界面只出现一次；长说明统一放到「字段聚焦/悬停」或常驻提示区，主表单保持精简。

### P10. placeholder 承担示例职责，且随上下文动态化

- **MS-Agent 实例**（v0.4.15/16）：各输入框 placeholder 承担「示例」价值（如 `如 glm-4-flash`）；v0.4.16 更进一步——**API Key 输入框 placeholder 随所选厂商+模型动态变化**：`粘贴 DeepSeek API Key（sk-...）· 模型 deepseek-v4-flash`、`粘贴 智谱 AI API Key（长串字母数字）· 模型 glm-4v-flash`、Groq `gsk_...`、OpenRouter `sk-or-...`、MiniMax `eyJ...(JWT)`。
- **简历生成助手 应用**：示例放 placeholder 不放按钮；凡是「格式随上下文变化」的输入框，placeholder 应跟着上下文走，而不是写死。

---

## 3. 实现模式（可直接借鉴的代码思路）

### 3.1 常驻动态引导行（railNote）

```html
<!-- 左栏进度条底部 -->
<div class="rail-note" id="railNote">开始：第 1 步 粘贴 API Key → 保存并自检</div>
```

```js
function setRailNote(html) {
  const el = document.getElementById("railNote");
  if (el) el.innerHTML = html;
}
```

4 个时机驱动状态切换（提交/保存自检成功/任务完成时各调一次 setRailNote）：开始态（页面加载默认）→ 下一步态（前置步骤验证通过）→ 生成中态（提交生成）→ 完成态（任务终态，失败则「部分完成：失败文件可单独重试」）。

### 3.2 必填项缺失定位引导

```js
if (missing.length) {
  els.inputError.textContent = "必填项未填写：" + missing.join("、") + "——已定位到第一个缺失项";
  // 按 岗位名称 → 简历 → JD 顺序找第一个缺失字段
  let firstMissing = null;
  if (!company) firstMissing = els.inpCompany;
  else if (!resume) firstMissing = els.dropResume;
  else firstMissing = els.inpJd;
  if (firstMissing) {
    firstMissing.classList.add("err-inp");                       // 红框高亮
    firstMissing.scrollIntoView({ behavior: "smooth", block: "center" }); // 滚动定位
    setTimeout(() => firstMissing.classList.remove("err-inp"), 2200);     // 2.2s 自动移除
  }
  return;
}
```

### 3.3 厂商 + 模型两级联动（含 Key 提示动态化）

```js
const PRESETS = {
  deepseek: {
    name: "deepseek", label: "DeepSeek",
    baseUrl: "https://api.deepseek.com/v1/chat/completions",
    keySample: "sk-...",                       // 该厂商 Key 格式示例
    models: [                                   // 只预置常见模型
      { id: "deepseek-v4-flash", note: "低成本、新用户送额度，日常够用" },
      { id: "deepseek-v4-pro", note: "更强推理，成本更高" }
    ]
  }
  // ...其余厂商各 3~4 个常见模型
};

function applyPreset() {          // 选厂商：填充模型下拉
  const p = PRESETS[selPreset.value];
  selModel.innerHTML = "";
  p.models.forEach(m => selModel.appendChild(
    Object.assign(document.createElement("option"), { value: m.id, textContent: m.note ? m.id + "（" + m.note + "）" : m.id })
  ));
  // 若之前选的模型仍属于该厂商则保留，否则回退第一个
  applyModel();
}

function applyModel() {           // 选模型：同步配置 + 动态化 Key 提示
  kpModel.value = selModel.value;
  kpKey.placeholder = "粘贴 " + p.label + " API Key（" + p.keySample + "）· 模型 " + selModel.value;
}
```

关键点：① 下拉 option 文案带简短说明（note），用户不用猜模型区别；② 联动是**单向**（下拉 → 配置输入框），用户可到折叠区手动改高级字段；③ 页面加载即 applyPreset 一次，默认厂商直接可用。

### 3.4 编号体系随结构更新

新增字段后全界面编号同步顺延（视觉版 ①选择厂商 ②选择模型 ③API Key ④模型能力 ⑤配置名称 ⑥模型名 ⑦接口地址；Lite 无能力组为 ④⑤⑥），**包括**设置抽屉里「获取方法见主页面ⓧ」的引用——这是最容易漏的一处。

### 3.5 流程指引（flow-steps：卡片级子流程步骤条）

配置卡片顶部放一条**步骤条**，把「配置 Key」这个子流程拆成可见步骤，用户照着序号做即可：

```html
<!-- 配置卡片顶部：子流程步骤条 -->
<div class="flow-steps">
  <div class="flow-step"><b><span class="fs-no">1</span>粘贴钥匙</b>选厂商 → 选模型 → 粘贴 API Key</div>
  <div class="flow-step"><b><span class="fs-no">2</span>选择能力</b>纯文本即可</div>
  <div class="flow-step"><b><span class="fs-no">3</span>保存验证</b>点「💾 保存并自检」，状态条变绿即成功</div>
  <div class="flow-step optional"><b><span class="fs-no alt">4</span>高级设置（可选）</b>多个模型 / 视觉模型 → 右上「⚙ 高级设置」</div>
</div>
```

要点：
- **每步 = 编号圆点 + 动作标题 + 一句话怎么做**。「粘贴钥匙」「保存验证」是**动作**不是概念名——用户照做即可，不用先理解「API Key 是什么」。
- 可选步骤用 `optional` 样式（黄色底纹 + 灰色编号 `fs-no alt`），与必做步骤视觉区分；完成态有 `.done` 绿色样式供 JS 随进度点亮。
- 视觉版 4 步（含能力选择），Lite 版 3 步（纯文本版无能力步）——**步骤条与产品能力严格对齐**，不出现本产品没有的步骤。
- 与 3.6 的页面级主线构成两级指引：步骤条管「这一个卡片怎么填完」，主线管「整个任务怎么走完」。

### 3.6 常驻提醒栏（右栏 help-box + HELP 字典）

主界面保持精简（P9）的所有长说明，统一放进右侧**常驻提醒栏**，聚焦 / 点击任意字段即切换内容：

```html
<!-- 右侧常驻提醒栏 -->
<div class="help-box">
  <div class="help-title" id="helpTitle">👋 欢迎使用 MS-Agent</div>
  <div class="help-body" id="helpBody">点击 / 聚焦中间任意输入项，右侧会显示该操作的详细说明。</div>
</div>
<div class="help-foot" id="helpFoot">使用主线：① 配置 API Key → ② 填写材料 → ③ 一键生成。</div>
```

```js
// 字段说明字典：t = 一句话标题，h = 正文（是什么 + 怎么填，可含 <b>/<code>/<ul>），f = 底部一行补充
const HELP = {
  selPreset:   { t: "① 选择厂商", h: "<p>…</p>", f: "选厂商与模型后粘贴 API Key 即可保存自检。" },
  kpKey:       { t: "🔑 API Key 是什么？", h: "<p>…</p>", f: "…" },
  __default__: { t: "👋 欢迎使用", h: "点击 / 聚焦任意输入项…", f: "使用主线：①…②…③…" } // 兜底态
};
function show(id) {
  const h = HELP[id] || HELP.__default__;
  helpTitle.innerHTML = h.t;
  helpBody.innerHTML = h.h;
  helpFoot.innerHTML = h.f;
}
document.addEventListener("focusin", e => {          // 键盘 Tab 聚焦也能命中
  if (e.target.id && HELP[e.target.id]) show(e.target.id);
});
Object.keys(HELP).forEach(id => {                     // 鼠标点击兜底（触屏/无焦点场景）
  if (id === "__default__") return;
  const el = document.getElementById(id);
  if (el) el.addEventListener("click", () => show(id));
});
show("__default__");                                  // 初始欢迎态
```

要点：
- **每条说明三段式**：t 一句话标题 → h 正文（是什么 + 怎么填 + 平台示例，可用 `<b>`/`<code>`/`<ul>`）→ f 一行补充（常见错误 / 成本 / 快捷路径）。
- **focusin 全局监听 + click 兜底**：键盘 Tab 聚焦和鼠标点击都能命中，无障碍也受益。
- **`__default__` 兜底**：未登记字段聚焦时显示欢迎 / 引导态，杜绝空白面板。
- 这就是「用户不读文档」的最终承载：主界面只有精简字段，**详细说明在用户"目光所在处"即时呈现**——说明书跟着光标走，而不是藏在文档里。

### 3.7 「大模型 API Key」板块设计（状态条 + 保存并自检）

主界面第 1 步卡片是配置类表单的样板：**状态总览 → 子流程步骤条 → 分层字段 → 保存并自检**。

**① 板块自顶向下分层**：

```
┌ 状态条 keyStatus      —— 全局配置状态总览（四态，见下）
├ 子流程步骤条 flow-steps —— 3.5：1 粘贴钥匙 → 2 选择能力 → 3 保存验证 → 4 高级设置（可选）
├ 第 1 组 选厂商+模型+粘贴钥匙 —— 3.3 两级联动（厂商/模型/API Key 三个主字段）
├ 第 2 组 选择能力类型        —— 视觉版专属（文本/识图）；纯文本版无此步（能力固定 text）
├ 折叠区 确认身份信息        —— 高级字段（配置名称/模型名/接口地址），自动填好后默认折叠
└ 主操作 💾 保存并自检        —— 两段式：先保存写入配置，再做真实最小请求验证
```

分层原则：**高频主字段（厂商/模型/Key）平铺可见，低频高级字段（名称/URL）折叠收纳**——用户 90% 场景只碰前两组 + 一个按钮。

**② 「保存并自检」= 两段式（不是只存不验，也不是只验不存）**：

```js
// ① 先保存：写入 config.json，返回最新 provider 快照
const pv = await api("/api/providers", { method: "POST", body: JSON.stringify(body) });
state.providers = pv.providers;      // 立即回写快照供状态条渲染
els.kpKey.value = "";                // 保存后清空 Key 输入框，不留敏感值
// ② 再自检：真实最小请求（real:true 才发请求，默认 real:false 只做配置结构检查）
const t = await api("/api/providers/" + encodeURIComponent(body.name) + "/test", { method: "POST", body: '{"real":true}' });
if (t.ok) { markVerified(body.name); setKeyMsg("ok", "✓ 已保存并自检通过：…"); }
else      { setKeyMsg("err", "已保存，但自检未通过：" + humanTestError(t.detail)); }
```

**③ 自检的真实性设计（后端 testProvider）**：

- `real:false`（默认）：只做配置结构可用性检查（enabled + apiKey + baseUrl + model + cap），**不发网络请求**——用于高级设置列表的快速状态展示。
- `real:true`：发一次最小请求，`askText("请只回复：OK", { maxTokens: 64, onlyProvider: name })`。三个关键细节：
  - **`onlyProvider: name`**——只测指定 provider，防止链上其他 provider「顶包」（曾踩坑：自检被 fallback 链顶包导致误判通过）。
  - **`maxTokens: 64`**——推理模型（如 DeepSeek-V4-Flash）的 reasoning 会吃 token，设 8 太小吃不下必致 content 为空（曾踩坑 S1）。
  - **`"请只回复：OK"`**——最小成本提示词。
- 视觉/多模态模型：自检只验证**文本连通性**，成功后明确告知「视觉能力当前流程不需要，未单独验证；模型不支持看图也不影响生成」——不做过度的能力验证承诺。

**④ 验证状态持久化（msa_verified）——绿色状态条的唯一来源**：

```js
// 只有真实请求通过才记入 localStorage；配置残留/假 Key 永远到不了绿色
function getVerified() { try { return JSON.parse(localStorage.getItem("msa_verified") || "{}"); } catch (e) { return {}; } }
function markVerified(name) { try { const v = getVerified(); v[name] = Date.now(); localStorage.setItem("msa_verified", JSON.stringify(v)); } catch (e) {} }
```

**⑤ 状态条四态机（renderKeyStatus）**——不同状态给不同引导动作，绝不只报「不对」：

| 态 | 条件 | 文案要点（含下一步动作） |
|---|---|---|
| 🟢 已就绪 | 有配置且 `msa_verified` 记录 | 「✓ 共 n 个可用（模型：…）→ 下一步：填写第 2 步内容，点一键生成」+ 左栏步骤点亮 + railNote 切「下一步」 |
| 🟡 待验证 | 有可用配置但从未真实通过 | 「已保存 n 个配置，但尚未验证真实可用。请点『保存并自检』…若失败按提示核对 Key/URL/模型」 |
| 🟡 缺能力 | 有配置但均无 text 能力 | 「均缺少文本生成能力（cap 需含 text）…请把『④ 模型能力』改为文本模型」 |
| 🟡 未配置 | 无任何配置 | 「尚未配置 API Key…请按上方引导：①选厂商 → ②选模型 → ③粘贴 Key → ④选能力 → 点保存自检」 |

关键：**每个状态都自带「下一步该做什么」**，这就是 P3/P4 在状态条上的落地；绿色态还联动左栏步骤「done」和 railNote 文案（状态机共享，不散落各处）。

**⑥ 错误翻译（humanTestError）——把错误码翻译成「下一步该做什么」**：

| 匹配 | 用户可行动提示 |
|---|---|
| `401/Unauthorized/invalid key` | API Key 无效或已过期——请回平台控制台重新复制 Key（注意复制完整、前后无空格） |
| `403` | 无权限访问——请检查平台账号权限 / 是否已实名 |
| `404` | 接口或模型不存在——请核对 Base URL 与模型名 |
| `429` | 请求过频或额度用尽——稍后重试，或检查平台余额 |
| `400` | 请求参数不被接受——请核对 Base URL 末尾是否为 /chat/completions |
| `timeout` | 请求超时——请检查网络后重试 |
| `缺 text 能力` | 该配置缺少文本生成能力（cap 需含 text），无法用于生成面试材料 |

原则：**错误提示永远落在「用户能做的动作」上**（重新复制 / 核对 / 重试），而不是回显技术错误码。

**⑦ 交互细节**：

- **字段级校验**：保存前逐个指出缺什么、去哪填（「缺少必填项：⑤ 配置名称、⑥ 模型名——可先在上方 ①选择厂商 + ②选择模型自动填入」），不用笼统「必填」。
- **输入即重置提示**：改动任一配置字段立即清掉上次的结果提示（成功/失败都不残留），避免误导「刚才的失败是我现在这个 Key 的」。
- **保存按钮 loading 禁用**：自检期间 disabled，防重复提交。
- **保存后 toast 汇总**：「已保存为配置「deepseek」（共 3 个），可在 ⚙ 高级设置 中查看 / 管理」——把单条快速配置与高级设置多配置管理衔接起来。
- **双入口分工**：第 1 步每次保存一个配置（快速路径，够用就好）；右上「⚙ 高级设置」抽屉统一管理多个（多平台备用切换 / 启停 / 删除 / 单独自检）——**「快速配单条」+「统一管多条」**，避免把多 provider 管理逻辑塞进第 1 步。

---

## 4. 验证方法论（怎么确认改动真的对了）

1. **浏览器自动化逐项验证**：每个版本列出 8~12 项可核验清单（控件存在性 / 默认值 / placeholder 精确匹配 / 联动值 / 禁用词 0 残留 / 无 JS 报错），PASS/FAIL 逐项记录。
2. **预期偏差分析**：初测发现「空表单点生成被 Key 校验前置拦截」——先分析是**产品正确行为**（一阶引导先配置 Key）还是 bug，再决定是否补测，不把预期偏差直接判 FAIL。
3. **注入假配置绕过前置条件**：功能验证受阻于真实网络校验时，临时写入假配置（如 `sk-fake-for-verification-only`）使校验通过后补测目标功能，测完清理临时文件（gitignored 不入库）。
4. **静态残留清零**：`node --check` 全部 JS + `grep` 断言禁用词（如「选平台/概念区分/两个名称别搞混」）在 UI 文件中 0 处。
5. **两版并行 + 端口串行**：两个产品共用 8900 端口且 server.js 硬编码——验证必须串行（先测 A 停掉再启 B），并注意静态资源实时读盘、服务进程无需重启。

---

## 5. 踩坑与教训（今天实际犯过的错）

| # | 教训 | 场景 |
|---|---|---|
| 1 | **编辑先加后删造成重复** | 给上传提示加了「扫描件请转文字版」，随后发现与右侧字段帮助重复（违反主界面精简原则），立即回退。教训：改文案前先全局 grep 同主题文案是否已存在 |
| 2 | **编号引用遗漏** | 新增「② 选择模型」后，主界面编号顺延，但设置抽屉里「获取方法见主页面⑥的说明」没同步，浏览器验证时才发现。教训：改结构后全仓库 grep 旧编号/旧表述 |
| 3 | **git add 中文路径 pathspec 不匹配** | 仓库路径含中文，`git add "MS-Agent/具体文件"` 报错；改用 `git add -u` 只加已跟踪文件。教训：此仓库提交一律 `git add -u` |
| 4 | **端口硬编码冲突** | Lite 版 server.js 硬编码 8900 与视觉版冲突，不支持 PORT 环境变量。教训：验证流程串行化，或改用可配置端口 |
| 5 | **「平台」→「厂商」统一口径遗漏** | 概念区分文字删除后，进度条/状态条/错误提示里仍有「选平台」表述，grep 时发现 4 处残留。教训：术语统一要用 grep 全量扫，不靠记忆 |

---

## 6. 简历生成助手 行动清单（建议立即执行的改进）

按优先级排序，均直接来自上述原则。**执行状态核对见 §8（2026-08-08）**：

1. **【P1】界面隐私声明清理**：检查简历工作台 web 界面（templates/app 前端）是否出现「所有数据保存在本机 / 数据不出机 / 不上传」等声明文案——移出 UI，保留在 README 与使用文档。→ **✅ 已满足（前端 0 处）**
2. **【P5/P6】配置交互升级**：简历生成助手 当前通过 config.json / .env 手改 base_url/model。升级为「厂商 + 模型」两级联动下拉（DeepSeek 等厂商预置常见模型），API Key 输入框 placeholder 随所选厂商+模型动态显示 Key 格式示例；删除界面上的「默认/推荐」字眼。→ **❌ 未执行（下一轮优先）**
3. **【P2/P3】常驻步骤引导**：简历工作台左栏/顶部加常驻引导行，随流程切换（开始 → 下一步 → 生成中 → 完成），让「用户不读文档」也能走完流程。→ **⚠️ 部分（已有 4 步静态引导条，未做动态状态切换）**
4. **【P4】必填缺失定位**：简历表单必填项（基本信息 / 教育 / 实习 / 项目 / JD）校验失败时滚动定位 + 红框高亮 + 「已定位到第一个缺失项」文案。→ **⚠️ 部分（仅保存设置时滚动，无红框高亮定位）**
5. **【P7/P8】伪选项与演示按钮排查**：删除界面上的伪选项（实际固定生效的字段）；主 CTA 旁不摆「示例/演示」按钮（示例价值由 placeholder 承担）。→ **✅ 已满足（无示例按钮）**
6. **【P9】提示收敛**：同一信息只出现一次，长说明统一到字段聚焦/常驻提示区。→ **⚠️ 部分（水印/搜索提示已收敛，全量核对未做）**
7. **【P10】placeholder 动态化**：所有「格式随上下文变化」的输入框，placeholder 跟随上下文。→ **❌ 未执行**
8. **验证纪律**：每个版本用浏览器自动化逐项验证（PASS/FAIL 清单）+ 禁用词 grep 清零 + 无 JS 报错，再提交；提交信息用专业动词开头（如 `feat: 厂商+模型两级联动选择`），避免「优化了…让…更…」式 AI 措辞。→ **✅ 已执行（每轮回归：logic_check / smoke / node --check / 浏览器验证）**

---

## 7. 附录：今天版本变更的完整记录

- 权威版本记录见 `D:\TRAE\WORKSPACE\MS-Agent-ALL\MSA进化路径.md`（v0.4.14 / v0.4.15 / v0.4.16 条目），含每条变更的**用户故事 / 实现要求 / 成功指标**，以及行业对标研究结论（面试类产品无示例按钮先例；表单 UX 三原则：错误恢复指出具体字段 / 引导界面内自解释 / 常驻步骤指示器）。
- GitHub：MS-Agent `9d8b119` / MS-Agent-Lite `3802b5c`（v0.4.16，均已推送）；v0.4.15 为 `0502482` / `0eb88ff`。

---

## 8. 简历生成助手 执行回执（2026-08-08 对照核对）

> 将 §6 行动清单逐条对照 简历生成助手 当前前端/后端实测（grep + 浏览器验证），并汇总 R19~R22 迭代中与本文原则对应的落地。未执行项即为下一轮改进的输入。

### 8.1 行动清单执行状态（证据来自 `frontend/index.html`、`frontend/js/app.js`、`frontend/css/app.css`）

| 行动 | 状态 | 证据 / 说明 |
|---|---|---|
| 1. 隐私声明清理（P1） | ✅ 已满足 | grep「数据不出机/不上传/仅保存在本机」在 `frontend/` 0 处；隐私说明仅存在于 README 与文档 |
| 2. 厂商+模型两级联动 + Key 提示动态化（P5/P6/P10） | ✅ 已执行（r23） | 「厂商」下拉预置 7 家（DeepSeek/硅基流动/OpenAI/智谱/通义/Moonshot + 自定义），选厂商自动填充 baseUrl/模型，Key placeholder 动态化（如「粘贴 DeepSeek API Key（sk-...）· 模型 deepseek-chat」）；已有配置按 baseUrl 反向回显厂商 |
| 3. 常驻步骤引导（P2/P3） | ✅ 已执行（r23） | 引导条改为应用流程 4 步（配置模型→填写简历→生成简历→预览/导出），`setFlow()` 随状态自动切换高亮：无配置=1、已配置=2、简历已保存=3、生成完成=4、失败/取消回退=3 |
| 4. 必填缺失定位（P4） | ✅ 已执行（r23） | 生成前 `checkRequired()` 校验（姓名/教育/技能/JD），失败滚动 + 红框高亮 2.2s + 「必填项未填写：…——已定位到第一个缺失项」；空行容器回退到「添加」按钮；浏览器实测 PASS |
| 5. 伪选项 / 示例按钮排查（P7/P8） | ✅ 已满足 | 无「填入示例/试试效果」按钮；表单伪选项已清理（能力选择 r16 已删）；placeholder 承担示例职责 |
| 6. 提示收敛（P9） | ✅ 已核对 | 水印说明、联网搜索说明均单点出现（生成卡片 / 抽屉各一处 + 教程 details），无重复需收敛 |
| 7. placeholder 动态化（P10） | ✅ 已执行（并入行动 2） | 见行动 2：Key placeholder 随厂商+模型动态变化 |
| 8. 验证纪律 | ✅ 已执行 | 每轮回归：`tests/logic_check.py`（133 通过）+ `tests/smoke_api.py`（52 通过）+ `node --check` + 浏览器逐项验证 |

### 8.2 R19~R22 迭代与本文原则的对应（近期新增能力）

| 迭代 | 能力 | 对应原则 |
|---|---|---|
| R19/R20 | 插件**「一键配置」**：依赖检测→自动安装→默认参数→基础功能预激活 | P2「引导界面内自解释」——把安装/配置等复杂动作封装为单一按钮，用户不读文档也能完成 |
| R20 | **配置成功与启用分离** + 三态状态标签（待接入→已配置→已启用） | P3 精神延伸——「状态指示明确当前功能启用状态」，让用户时刻知道该做什么 |
| R20 | **Tavily 教程 details 模块**（获取 Key 步骤 + 官方链接直达） | P2/P3——教程折叠收纳，不占主界面，需要时展开 |
| R20 | **水印选项逐项说明**（正式无水印 / 练习带 AI 提示） | P9「提示精简、效果说明明确」 |
| R21/R22 | **免环境分发**（EXE 双击即用 / 便携 ZIP）+ 版本更新检查 | P2 下沉到分发层——「开箱即用，免于配置环境」；版本机制对应 §4 验证纪律的迭代闭环 |
| R18 | 高级设置**抽屉**（topbar 常驻按钮 + 右侧抽屉） | P9 精神——主界面精简，设置收敛到抽屉 |

### 8.3 下一轮行动

> 2026-08-08 二次更新：§6 全部 8 项已核对完毕，其中 **4 项在 r23 落地**（行动 2/3/4/7），其余已满足。剩余候选改进（低优先，待有需求再启动）：
>
> 1. 【低】厂商预置表扩源：PRESETS 增加更多厂商（如 Groq / OpenRouter / MiniMax / Anthropic），模型下拉随厂商数据更新。
> 2. 【低】模型名联动：模型下拉化（当前为文本输入，选厂商时自动填首个模型，仍可手改）。
