# 简历生成助手设计规范（Design System）

> 依据《05_UI-UX设计师.md》§七与《12_美术风格设计师.md》§七交付标准编制。实现基准：`frontend/css/app.css`。变更需同步本文件 + `docs/CHANGELOG.md`。

## 0. 品牌调性 → 视觉元素映射（VIS P2-1）

> 视觉依据：任何风格决策须能回溯到本表（0 次「凭空风格」）。

| 调性关键词 | 视觉元素 | 实现位置 |
|---|---|---|
| 专业 / 可信 | 深色顶栏 #111827 + 浅灰工作台 #f2f4f8 + 卡片描边 | --topbar / --bg / .card |
| 主行动明确 | 品牌蓝 #2563eb 唯一主 CTA 色，白字 ≥5:1 | --accent / .btn.primary |
| AI 赋能 | accent→accent-2 蓝青渐变（进度条/步骤圆点） | --accent / --accent-2 |
| 清爽克制 | 大面积留白、线性图标（feather 风格）、无装饰图案 | --sp-* / .ico |
| 可信反馈 | 语义色统一收编 tokens；状态均配文字（色盲安全） | --ok-* / --warn-* / --danger-* |

## 1. 设计 Tokens（色板）

> 使用约定：**组件中禁止硬编码颜色**，一律引用下表变量（近色合并容忍微差）。

### 1.1 中性色
| Token | 值 | 用途 |
|---|---|---|
| `--bg` | #f2f4f8 | 页面背景 |
| `--card` | #ffffff | 卡片/面板 |
| `--bg-soft` | #f1f5f9 | 浅灰信息底（rail-note/tag.optional） |
| `--bg-input` | #fafbfc | 输入框底 |
| `--ink` | #1f2937 | 主文字 |
| `--ink-soft` | #4b5563 | 次要文字（≥AA） |
| `--ink-mute` | #6b7280 | 弱化文字（≥4.5:1） |
| `--muted` | #94a3b8 | 禁用/占位级（不用于正文） |
| `--line` | #e5e7eb | 边框 |
| `--line-soft` | #eef0f3 | 浅分割 |

### 1.2 品牌/主行动
| Token | 值 | 用途 |
|---|---|---|
| `--accent` | #2563eb | 主行动（按钮/链接/焦点） |
| `--accent-2` | #06b6d4 | 渐变副色（进度/圆点） |
| `--accent-soft` | #eff6ff | 主色浅底 |
| `--accent-bg` | #dbeafe | 主色描边 |
| `--info-border` | #bfdbfe | 激活态描边 |
| `--info-ink` | #1e4e8c | 信息提示文字 |

### 1.3 语义色（状态）
| Token | 值 | 用途 |
|---|---|---|
| `--ok` / `--ok-bg` / `--ok-border` / `--ok-ink` | #059669 / #ecfdf5 / #a7f3d0 / #047857 | 成功（edit-tip.show/tag.ok-tag；`--ok-ink` 为小字深档，对比度 5.2:1） |
| `--warn` / `--warn-bg` / `--warn-border` / `--warn-ink` | #d97706 / #fffbeb / #fde68a / #92400e | 警告（banner） |
| `--err` / `--danger-bg` / `--danger-border` / `--danger-ink` | #dc2626 / #fef2f2 / #fca5a5 / #b91c1c | 错误/危险（err-inp/login-notice/btn.danger） |
| `--code-bg` / `--code-ink` | #eef1f5 / #c0341d | 行内代码 |
| `--cat-bg` / `--cat-ink` | #e0e7ff / #4f46e5 | 分类标签 |

### 1.4 顶栏
| Token | 值 |
|---|---|
| `--topbar` | #111827 |
| `--topbar-fg` | #f9fafb |

### 1.5 圆角/阴影/字体
| Token | 值 | 用途 |
|---|---|---|
| `--radius-card` / `--radius-input` / `--radius-small` / `--radius-badge` | 12 / 8 / 6 / 16px | 卡片/输入/小件/胶囊 |
| `--shadow-card` | 0 1px 3px rgba(0,0,0,.08)… | 卡片投影 |
| `--font` | 系统栈 + PingFang/雅黑 | 全局字体 |

### 1.6 字阶系统（VIS P1-2，6 级收敛）
| Token | 值 | 用途 |
|---|---|---|
| `--fs-lg` | 16px | 顶栏品牌 |
| `--fs-title` | 15px | 卡片/弹窗标题 |
| `--fs-body` | 14px | 正文 |
| `--fs-sub-2` | 13px | 分组摘要/按钮/次要主文字 |
| `--fs-sub` | 12.5px | label/流程文字 |
| `--fs-mini` | 12px | 弱化说明/胶囊（最小字号，≥AA 色） |

> 红线：禁止出现 tokens 之外的字号（原 11px/11.5px 已提至 12px 保可读性）。

### 1.7 间距基准（VIS P1-2，4px 网格）
| Token | 值 | 用途 |
|---|---|---|
| `--sp-1` / `--sp-2` / `--sp-3` | 4 / 8 / 12px | 内边距/间隙微调 |
| `--sp-4` / `--sp-5` / `--sp-6` | 16 / 20 / 24px | 卡片 padding / 布局 gap / 顶栏 |

> 主体组件（card/fs-fold/btn/help-box/actions/layout/drawer）已引用间距 tokens；不规则微调值（9/10/13/18px 等）可保留但新代码一律用 `--sp-*`。

## 2. 组件规范

| 组件 | 关键规则 |
|---|---|
| 按钮 `.btn` | 默认白底描边；`.primary` 主色实底白字；`.danger` 红字红边；hover 亮度微升；disabled 半透明 |
| 卡片 `.card` | 白底 + 1px 描边 + 圆角 12 + 阴影；头部 `h2` 15px/700 |
| 输入 `.grid input/select/textarea` | 灰底 #fafbfc；focus 蓝色 3px ring + 白底；宽度 100%（checkbox 例外） |
| 标签 `.tag` | 胶囊圆角 16；required=危险浅底 / optional=灰浅底 / ok=成功浅底 / on=主色实底 |
| 横幅 `.banner` | 警告语义（浅黄底/描边/深黄字）；错误用 `showBanner(msg, true)` 加红字 |
| 进度 `.progress-*` | 8px 圆角条 + accent→accent-2 渐变填充 |
| 折叠分组 `details.fs-fold` | 1px 描边 + summary 600 字重 + ▸ 箭头（open 旋转 90°） |
| 辅助栏 `.rail` | 左 224px/右 272px（sticky）；1240/1024/820 响应式收起 |
| 提醒栏 `.help-*` | 三段式：标题 13.5px/700 + 正文 12px + 脚注虚线分隔 |
| 弹窗 `.modal` | 遮罩 rgba(15,23,42,.45)；内容 560px/92vw + 大阴影 |

## 3. 排版层级

| 级别 | Token | 字号/字重 | 用途 |
|---|---|---|---|
| 顶栏品牌 h1 | `--fs-lg` | 16px/700 | 顶栏品牌 |
| 卡片标题 h2 | `--fs-title` | 15px/700 | 分区标题 |
| 分组 summary / 按钮 | `--fs-sub-2` | 13px/600 | 折叠分组 / 按钮文字 |
| 正文 | `--fs-body` | 14px/400 | 默认 |
| 次要 | `--fs-sub` | 12.5px/600 | label 文字 |
| 弱化 | `--fs-mini` | 12px | 说明/时间戳（≥AA 色，禁用 <12px） |

## 4. 响应式断点

| 断点 | 行为 |
|---|---|
| ≤1280 | 布局 max-width 1280 居中 |
| ≤1240 | 左 190px / 右 244px |
| ≤1024 | 隐藏右提醒栏 |
| ≤820 | 隐藏左流程栏 |
| ≤720 | grid 单列 |

## 5. 可访问性基线（WCAG AA）

- 正文对比度 ≥4.5:1；弱化文字最低 `--ink-mute`（禁止 `--muted` 用于正文）
- 所有可交互元素：`focus-visible` 2px 主色 outline（输入框用 3px box-shadow ring）
- Esc 关闭抽屉/弹窗；抽屉打开焦点落首个控件
- 状态不得仅靠颜色传达（配文字/图标）

## 6. 使用红线

1. 新增颜色必须进 `:root` tokens，禁止散落硬编码；
2. 组件样式优先复用（btn/tag/banner/progress），不复制变体；
3. 文案保持「真实优先」口径（降级标注【待联网核实】【待补充】）。
